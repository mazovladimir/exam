p= link_to 'Создать вопрос', new_question_path
p= link_to 'Начать экзамен', user_questions_path(user_id: @user.id), method: :post if user_signed_in?

div class='myquestions'

  = render @user.questions
